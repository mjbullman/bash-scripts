# shellcheck shell=bash
# shellcheck disable=SC2034
# macOS-only packages. These arrays extend the shared categories —
# the installer concatenates them with the shared arrays.

ESSENTIALS_MACOS=(
    tree-sitter             # Incremental parsing system (used by neovim plugins)
)

SHELL_ENHANCE_MACOS=(
    starship                # Cross-shell prompt
    fastfetch               # System info with ASCII art
    bat-extras              # Extra utilities for bat (batman, batgrep, etc.)
)

TERMINAL_TUIS_MACOS=(
    lazygit                 # Git TUI
    lazydocker              # Docker TUI
)

LANG_TOOLING_MACOS=(
    jenv                    # Java version manager
    fnm                     # Fast Node version manager
    rustup                  # Rust toolchain manager (provides rustc/cargo)
    stylua                  # Lua formatter
)
