# shellcheck shell=bash
# shellcheck disable=SC2034
# macOS-specific packages. These arrays *extend* the shared categories
# defined in packages.shared.conf — the installer concatenates them.

LANG_TOOLING_MACOS=(
    jenv                   # Java version manager
)
